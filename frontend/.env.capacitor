# ── Build nativo (Capacitor / Android) ──────────────────────────────────────
# Se carga solo con `vite build --mode capacitor` (ver script "build:android").
# No afecta `npm run dev` ni `npm run build` normales (esos usan .env.local / .env).

VITE_API_BASE_URL=https://refactor.kyrocodelabs.cloud/api
VITE_APP_NAME=Asistencia Mundo Android
