/**
 * Versión del APK empaquetado en este build (Capacitor Android). Se compara
 * contra `/v1/app-terminal/version` (APP-09a) para avisar de actualizaciones —
 * subir esta constante en cada build que se publique con `AppTerminalController::subir`.
 */
export const APP_VERSION = '1.0.0'
